import { defineConfig } from 'vite'
import path from 'path'
import tailwindcss from '@tailwindcss/vite'
import react from '@vitejs/plugin-react'


function figmaAssetResolver() {
  return {
    name: 'figma-asset-resolver',
    resolveId(id) {
      if (id.startsWith('figma:asset/')) {
        const filename = id.replace('figma:asset/', '')
        return path.resolve(__dirname, 'src/assets', filename)
      }
    },
  }
}

export default defineConfig({
  // Use relative asset paths by default so the build works from GitHub Pages,
  // Capacitor, and other static hosts without depending on a fixed subpath.
  base: process.env.VITE_BASE_PATH || './',
  optimizeDeps: {
    // Restrict entry scanning to the app root to avoid pulling generated
    // Android asset HTML files into dependency optimization.
    entries: ['index.html'],
  },
  plugins: [
    figmaAssetResolver(),
    // The React and Tailwind plugins are both required for Make, even if
    // Tailwind is not being actively used – do not remove them
    react(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src/app'),
    },
  },
})
